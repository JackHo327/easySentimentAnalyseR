# go to the website of tm package to know its detailed information
if(!require(tm, quietly = T, warn.conflicts = T)) install.packages("tm",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(tm, quietly = T, warn.conflicts = T)

# go to the website of XML package to know its detailed information
if(!require(XML, quietly = T, warn.conflicts = T)) install.packages("XML",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(XML, quietly = T, warn.conflicts = T)

# go to the website of twitteR package to know its detailed information
if(!require(twitteR, quietly = T, warn.conflicts = T)) install.packages("twitteR",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(twitteR, quietly = T, warn.conflicts = T)

# go to the website of httr package to know its detailed information
if(!require(httr, quietly = T, warn.conflicts = T)) install.packages("httr",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(httr, quietly = T, warn.conflicts = T)

# go to the website of magrittr package to know its detailed information
if(!require(magrittr, quietly = T, warn.conflicts = T)) install.packages("magrittr",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(magrittr, quietly = T, warn.conflicts = T)

# go to the website of stringr package to know its detailed information
if(!require(stringr, quietly = T, warn.conflicts = T)) install.packages("stringr",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(stringr, quietly = T, warn.conflicts = T)

# go to the website of wordcloud package to know its detailed information
if(!require(wordcloud, quietly = T, warn.conflicts = T)) install.packages("wordcloud",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(wordcloud, quietly = T, warn.conflicts = T)

# go to the website of tmcn package to know its detailed information
if(!require(tmcn, quietly = T, warn.conflicts = T)) install.packages("tmcn",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(tmcn, quietly = T, warn.conflicts = T)

# go to the website of SnowballC package to know its detailed information
if(!require(SnowballC, quietly = T, warn.conflicts = T)) install.packages("SnowballC",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(SnowballC, quietly = T, warn.conflicts = T)

# go to the website of RColorBrewer package to know its detailed information
if(!require(RColorBrewer, quietly = T, warn.conflicts = T)) install.packages("RColorBrewer",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(RColorBrewer, quietly = T, warn.conflicts = T)

# go to the website of dendextend package to know its detailed information
if(!require(dendextend, quietly = T, warn.conflicts = T)) install.packages("dendextend",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(dendextend, quietly = T, warn.conflicts = T)

# go to the website of qdap package to know its detailed information
if(!require(qdap, quietly = T, warn.conflicts = T)) install.packages("qdap",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(qdap, quietly = T, warn.conflicts = T)

# go to the website of openNLP package to know its detailed information
if(!require(openNLP, quietly = T, warn.conflicts = T)) install.packages("openNLP",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(openNLP, quietly = T, warn.conflicts = T)

# go to the website of NLP package to know its detailed information
if(!require(NLP, quietly = T, warn.conflicts = T)) install.packages("NLP",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(NLP, quietly = T, warn.conflicts = T)

# go to the website of rJava package to know its detailed information
if(!require(rJava, quietly = T, warn.conflicts = T)) install.packages("rJava",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(rJava, quietly = T, warn.conflicts = T)

# go to the website of data.table package to know its detailed information
if(!require(data.table, quietly = T, warn.conflicts = T)) install.packages("data.table",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(data.table, quietly = T, warn.conflicts = T)

# go to the website of devtools package to know its detailed information
if(!require(devtools, quietly = T, warn.conflicts = T)) install.packages("devtools",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(devtools, quietly = T, warn.conflicts = T)

# go to the website of syuzhet package to know its detailed information
if(!require(syuzhet, quietly = T, warn.conflicts = T)) devtools::install_github("mjockers/syuzhet")
require(syuzhet, quietly = T, warn.conflicts = T)

# go to the website of ggplot2 package to know its detailed information
if(!require(ggplot2, quietly = T, warn.conflicts = T)) install.packages("ggplot2",repos = "https://cloud.r-project.org/",quiet = TRUE)
require(ggplot2, quietly = T, warn.conflicts = T)
